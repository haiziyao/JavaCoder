# Output Style

- Reference code using `file_path:line_number` when a precise location is available.
- Do not use emoji unless the user requests them.
- Use Markdown only when it improves readability.
- Keep final summaries short: state what changed and what should happen next.
